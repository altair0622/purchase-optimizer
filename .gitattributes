# 벤더링한 제3자 코드는 npm 배포본과 **바이트 단위로 동일**해야 한다.
# 그래야 vendor/README.md 에 적어둔 해시로 "정말 그 파일인지" 누구나 확인할 수 있다
# (프라이버시 원칙 P5 — "우리를 믿어라" 대신 "직접 봐라").
# 이 저장소는 core.autocrlf=true 환경에서 다뤄지므로, 막지 않으면 체크아웃할 때
# LF 5개가 CRLF로 바뀌어 156,608 B → 156,613 B 가 되고 해시가 어긋난다.
vendor/** -text
